# 小米便签开源软件的维护需求与设计方案

## 一、维护需求描述

软件维护是软件系统开发中的重要环节。针对“小米便签”开源软件在实际使用中的体验问题，本次维护工作以**改善性维护**为主，兼顾**纠正性维护**与**适应性维护**，目标是提升功能完整性、交互稳定性与系统兼容性。

结合当前版本的业务现状，存在以下待完善点：

1. 用户创建的便签缺少访问保护能力，隐私性不足；
2. 受保护便签在“打开、删除”等关键操作中需统一访问控制；
3. 编辑页“更多(⋮)”菜单在部分 ROM 上存在 cancel/back 后失效问题；
4. 列表多选退出后在部分机型可能残留 ActionMode 底栏白条；
   据此，本次维护形成如下核心需求：

- （1）设置便签访问密码：支持对单条便签设置访问密码；
- （2）解除便签访问密码：支持校验后解除密码；
- （3）按密码访问便签：访问受保护便签时必须先验证密码；
- （4）受保护状态可视化：列表中清晰显示“锁”标识；
- （5）删除前鉴权：受保护便签删除前必须通过验证；
- （6）菜单稳定性提升：保证“更多(⋮)”在 cancel/back 后仍可重复使用；
- （7）多选模式稳定退出：退出后不残留底部白条；

---

## 二、软件完善性设计

本节从受影响分层出发，给出维护后的结构与关键设计。

### 1. 界面层“ui”包的调整

界面层以 `NoteEditActivity` 与 `NotesListActivity` 为核心，主要进行以下维护：

- 在 `NoteEditActivity` 中增加访问密码相关流程：
  - 打开受保护便签时先验证，再进入编辑；
  - 菜单动态显示“设置访问密码 / 解除访问密码”；
  - 删除前按需进行密码验证。
- 在 `NotesListActivity` 中完善批量删除鉴权：
  - 若选中集合包含受保护便签，逐条验证后才执行删除；
  - 增强多选模式退出逻辑，支持顶部取消、菜单取消、返回键退出。
- 编辑页“更多(⋮)”菜单稳定性改造：
  - 将系统 `openOptionsMenu()` 改为 `PopupMenu`；
  - 复用 `onPrepareOptionsMenu(Menu)` 的动态菜单规则；
  - dismiss 后统一复位按钮状态，避免后续不可点击。

**受影响主要类：**

- `NoteEditActivity`
- `NotesListActivity`
- `NotesListItem`

**图 1 经维护后的“ui”程序包设计示意图（类关系图）**

> 说明：图中可包含 `NoteEditActivity`、`NotesListActivity`、`NotesListItem` 与菜单/密码对话流程的关联关系。

### 2. 界面资源层“res”包的调整

资源层围绕密码输入、菜单入口与列表可视化进行完善：

- 布局资源：
  - `note_edit.xml`：增加“更多(⋮)”按钮入口；
  - `note_item.xml`：新增锁图标视图并按状态显示/隐藏；
  - `note_encryption.xml`、`note_encryption_repeat.xml`：用于设置密码与二次确认输入。
- 菜单资源：
  - `note_edit.xml`：增加设置/解除访问密码菜单项；
  - `note_list_options.xml`：补充多选模式取消项（兼容底部 ActionMode 样式）。
- 文案资源：
  - `strings.xml`、`values-zh-rCN/strings.xml`：新增密码提示、错误提示与“更多”入口文案。

**图 2 经维护后的“res”程序包设计示意图**

> 说明：图中可标注 `layout`、`menu`、`values`、`drawable` 与新增资源文件对应关系。

### 3. 模型层“model”包的调整

模型层重点完善“空便签可生成 noteId”的能力，以支撑密码功能闭环：

- `WorkingNote` 新增 `ensureNoteCreated()`：
  - 当便签尚未入库时先创建记录，确保后续设置/验证/解除密码都具备稳定 `noteId`。
- 统一与界面层的状态交互：
  - 在受保护便签加载、删除等关键节点，配合 Activity 做前置校验与状态控制。

**图 3 经维护后的“model”程序包设计示意图**

> 说明：图中可展示 `WorkingNote` 与数据访问对象之间的调用关系。

### 4. 数据层“data”与兼容性调整

本次维护中，密码数据采用轻量落地方案，不直接修改数据库结构：

- 使用 `SharedPreferences` 存储密码哈希：
  - 文件：`pref_access_password`
  - 键：`access_password_hash_<noteId>`
  - 值：`SHA-256(password)` 十六进制字符串
- 设计理由：
  - 便于在不做表结构迁移的前提下快速交付；
  - 与现有便签主数据解耦，降低升级风险。

同时完成 Android 12+ 兼容改造：

- 对闹钟、挂件、通知等场景的 `PendingIntent` 统一补齐 mutability flag；
- 主要采用 `FLAG_UPDATE_CURRENT | FLAG_IMMUTABLE`。

**图 4 经维护后的“data/兼容”设计示意图**

> 说明：图中可展示 `SharedPreferences` 与 `noteId` 的映射关系，以及 `PendingIntent` 兼容策略落点。

### 5. 交互稳定性专项设计

#### 5.1 编辑页“更多(⋮)”菜单稳定性

问题现象：

- 菜单项点击后弹出对话框，点 cancel 后再次打开菜单无响应；
- 菜单打开后直接返回，后续菜单按钮不可用。

方案：

- 使用 `PopupMenu` 替代系统 options panel；
- 菜单点击继续复用 `onOptionsItemSelected(MenuItem)` 业务处理；
- `PopupMenu` dismiss 回调中统一执行 `resetMoreMenuButton()`。

#### 5.2 列表多选退出“白条”问题

问题现象：

- 部分 ROM 在 ActionMode 结束后残留底部栏位或空白条。

方案：

- 对候选视图（如 `split_action_bar`、`action_mode_bar`）执行：
  - `setVisibility(View.GONE)`
  - `LayoutParams.height = 0`
  - `requestLayout()`
- 叠加 `postDelayed` 二次兜底与 `onResume/onWindowFocusChanged` 兜底隐藏；
- 增加调试日志用于定位视图来源，避免误伤内容区。

---

## 三、维护改动清单（按工程路径）

### 1. 主要 Java 类

- `app/src/main/java/net/micode/notes/ui/NoteEditActivity.java`
- `app/src/net/micode/notes/ui/NoteEditActivity.java`
- `app/src/main/java/net/micode/notes/ui/NotesListActivity.java`
- `app/src/net/micode/notes/ui/NotesListActivity.java`
- `app/src/main/java/net/micode/notes/ui/NotesListItem.java`
- `app/src/main/java/net/micode/notes/model/WorkingNote.java`

### 2. 主要资源文件

- `app/src/main/res/layout/note_edit.xml`
- `app/src/main/res/layout/note_item.xml`
- `app/src/main/res/layout/note_encryption.xml`
- `app/src/main/res/layout/note_encryption_repeat.xml`
- `app/src/main/res/layout/note_list_dropdown_menu.xml`
- `app/src/main/res/menu/note_edit.xml`
- `app/src/main/res/menu/note_list_options.xml`
- `app/src/main/res/values/strings.xml`
- `app/src/main/res/values-zh-rCN/strings.xml`

### 3. Android 12+ 兼容相关

- `app/src/main/java/net/micode/notes/ui/AlarmInitReceiver.java`
- `app/src/net/micode/notes/ui/AlarmInitReceiver.java`
- `app/src/main/java/net/micode/notes/widget/NoteWidgetProvider.java`
- `app/src/net/micode/notes/widget/NoteWidgetProvider.java`
- `app/src/main/java/net/micode/notes/gtask/remote/GTaskASyncTask.java`
- `app/src/net/micode/notes/gtask/remote/GTaskASyncTask.java`

### 4. 双源集同步约束

工程同时包含：

- `app/src/main/java/net/micode/notes/...`
- `app/src/net/micode/notes/...`

维护期间所有核心改动需双路径同步，避免出现“编译通过但运行到旧逻辑”的不一致问题。

---

## 四、验收标准与测试步骤

1. 新建便签并设置访问密码，系统提示设置成功；
2. 返回列表后，该便签显示锁图标；
3. 打开该便签需输入密码，错误密码有提示，正确密码可进入；
4. 解除访问密码后，列表锁图标消失，再次进入不再要求验证；
5. 编辑页删除受保护便签时，需先通过密码验证；取消验证则不删除；
6. 列表多选删除包含受保护便签时，逐条验证通过后才执行删除；
7. 菜单稳定性验证：
   - 点击“更多(⋮)”进入菜单，选择任意项后 cancel，再次打开菜单仍可点击；
   - 打开菜单后直接返回，再次点击“更多(⋮)”仍可正常使用；
8. 多选模式稳定性验证：进入并退出多选后，底部不残留白条或异常栏位；

---

## 五、结论

本次维护在不改变核心业务架构的前提下，完成了“便签访问密码”从功能到交互再到兼容性的闭环建设。维护结果不仅补齐了隐私访问控制能力，也显著提升了菜单交互稳定性与多机型一致性，为后续版本持续演进提供了可复用的实现基础。
